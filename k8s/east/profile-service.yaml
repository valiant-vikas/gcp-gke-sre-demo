apiVersion: v1
kind: Service
metadata:
  name: profile-service
  namespace: default
  labels:
    app: profile-service
spec:
  type: ClusterIP
  selector:
    app: profile-service
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP